cmake_minimum_required(VERSION 2.8)
project( convertScaleAbs )
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")
find_package( OpenCV REQUIRED )
add_executable( convertScaleAbs convertScaleAbs.cpp )
target_link_libraries( convertScaleAbs ${OpenCV_LIBS} )
