cmake_minimum_required(VERSION 2.8)
project( bitwise_not )
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")
find_package( OpenCV REQUIRED )
add_executable( bitwise_not bitwise_not.cpp )
target_link_libraries( bitwise_not ${OpenCV_LIBS} )
