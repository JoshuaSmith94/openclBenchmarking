cmake_minimum_required(VERSION 2.8)
project( mulTransposed )
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")
find_package( OpenCV REQUIRED )
add_executable( mulTransposed mulTransposed.cpp )
target_link_libraries( mulTransposed ${OpenCV_LIBS} )
